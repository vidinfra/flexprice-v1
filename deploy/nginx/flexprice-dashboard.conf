# FlexPrice Dashboard - Nginx Configuration
# Copy to /etc/nginx/sites-available/flexprice-dashboard
# Then: ln -s /etc/nginx/sites-available/flexprice-dashboard /etc/nginx/sites-enabled/
# Then: sudo certbot --nginx -d your-dashboard-domain.com

server {
    server_name billing.tenbyte.io;

    location / {
        proxy_pass http://127.0.0.1:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    listen 80;
}

# After running certbot, SSL config will be added automatically
